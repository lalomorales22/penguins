---
summary: "CLI reference for `penguins uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `penguins uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
penguins uninstall
penguins uninstall --all --yes
penguins uninstall --dry-run
```
